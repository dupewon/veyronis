[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "veyronis"
version = "0.1.0"
description = "Python SDK for Veyronis Universal Verifiable Security Behavior Engine"
authors = [{ name = "dupewon", email = "whuq@cheatglobal" }]
license = { text = "Apache-2.0 OR MIT" }
readme = "README.md"
requires-python = ">=3.8"
dependencies = []

[project.urls]
Homepage = "https://github.com/dupewon/veyronis"
Repository = "https://github.com/dupewon/veyronis"
